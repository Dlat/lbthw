a series of objects all of which are the same size and type. Each object in an array is called an arrayt element. For example, you could have an array of integers or an array of characters or an array of anything that has a defined data type
